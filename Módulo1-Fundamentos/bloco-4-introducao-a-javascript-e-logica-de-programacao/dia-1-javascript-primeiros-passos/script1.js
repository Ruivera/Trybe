const candidato = 64;

if (candidato >= 80) {
    console.log("Parabéns, você foi aprovada(o)!");
} else if (60 <= candidato && candidato < 80) {
    console.log("Você está na nossa lista de espera");
} else {
    console.log("Você foi reprovada(o)");
}

/* Para colocar duas condições dentro do if use os
 operadores lógicos */
